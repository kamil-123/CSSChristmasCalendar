<nav class="navbar navbar-expand-sm sticky-top navbar-dark bg-primary">
    <div class="container">
        <a href={{ path('admin_index') }} class="navbar-brand"><img class="logo" src="{{ asset('/images/logo.jpg') }}" width="30" height="30">Adventni kalendar</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <div class="dropdown-toggle mytoplink" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ app.user.username }}
                    </div>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('home') }}">Home</a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Odhlášení</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>